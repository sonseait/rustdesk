# Compatibility Matrix

| Component | Supported managed protocol |
| --- | --- |
| Client | v1 |
| `hbbs` | v1 |
| `hbbr` | v1 rollout pending relay enforcement |
| Control plane | v1 |

Legacy clients remain supported only while the server managed mode is `off` or
`optional`.
